Place static files for `display_firefly` docs, such as images and sample data, in this directory.

They can be referenced in reStructuredText using absolute paths, e.g.:

```
.. image:: /_static/display_firefly/image.jpg
```
